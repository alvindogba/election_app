<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liberia Voting Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="css/style.css">
    
</head>
<body>
    <header>
        <h1>Your One Vote Can Make Liberia Better! Vote Wisely</h1>
    </header>
    <main>
        <section class="voting-form-section">
            <h2>Cast Your Vote</h2>
            <form id="votingForm" action="/cast_vote_complete" method="post" onsubmit="return validateForm()">
                <input type="hidden" name="userId" value="<%= userId %>">
                <div class="candidate_list_container">
                    <% candidates.forEach(candidate => { %>
                        <div class="flex-column align-items-center" >
                            <img src="<%= candidate.photo %>" alt="Candidate Photo" width="300" height="300">
                            <label>
                                Vote for <%= candidate.first_name %> <%= candidate.middle_name %> <%= candidate.last_name %>
                                <p>Position:</p>
                                <p>Party:</p>
                                <div><input type="radio" name="candidate_id" value="<%= candidate.id %>"></div>
                             
                            </label>
                        </div>
                    <% }); %>
                </div>
                
                <button type="submit">Submit Vote</button>
            </form>
            <p id="resultMessage"></p>
        </section>
    </main>
   
    <%- include("patials/footer") %>
    <script src="js"></script>
</body>
</html>
